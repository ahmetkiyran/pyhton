# demetler
# demetlerin listelerden farkı değiştirilememesidir.

demet = (1,2,3,4,5,6,7,8)
print(type(demet))
print(demet[:4])
print(demet[::-1])

demet = (1,1,1,1,2,3,4,5,5,6,7,6,5,)
print(demet.count(1))# count un içine yazdığımız değer demetin içinde kaç tane geçiyorsa onu gösterir.

demet = ("python","php","c","java")
print(demet.index("python"))# indexin içine yazdığımız elemanın demetin kaçıncı indexinde bulunduğunu söyler.
